generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Application {
  id             String   @id @default(cuid())
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  // Basic applicant info
  firstName      String?
  lastName       String?
  email          String?
  phone          String?

  // Deal info
  vehicleYear    Int?
  vehicleMake    String?
  vehicleModel   String?
  vehiclePrice   Float?
  downPayment    Float?
  tradeIn        Float?
  amountFinanced Float?

  // Credit / income
  creditScore    Int?
  monthlyIncome  Float?

  // Workflow
  status         String   @default("PENDING")

  // ✅ UNDERWRITING (NEW — THIS IS THE WHOLE POINT)
  tier           String?
  lender         String?
  maxPayment     Float?
  maxVehicle     Float?
  decisionReason String?
}
